import { ClientConnection } from 'message-event-channel';
export declare class HandlerContainer<Handler extends (model: any) => void, Config = {}> {
    event: string | null;
    connection: ClientConnection;
    changeHandlers: Map<string, Set<Handler>>;
    constructor(event: string | null, connection: ClientConnection);
    add(cb: Handler, config?: Config): () => void;
    run<Model>(model: Model, handlerId?: string): void;
}
export declare const HandlerContainerFactory: (connection: ClientConnection) => <Handler extends (model: any) => void, Config = {}>(event?: string | null) => HandlerContainer<Handler, Config>;
