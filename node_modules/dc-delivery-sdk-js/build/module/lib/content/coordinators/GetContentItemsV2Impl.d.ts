import { AxiosInstance } from 'axios';
import { ContentClientConfigV2 } from '../../config';
import { FetchRequest, FetchResponse } from '../model/Fetch';
/**
 * Wraps `content/fetch`
 * @hidden
 */
export declare class GetContentItemsV2Impl {
    private readonly config;
    private readonly contentClient;
    constructor(config: ContentClientConfigV2, contentClient: AxiosInstance);
    getContentItemsByKey<Body>(keys: Array<string>): Promise<FetchResponse<Body>>;
    getContentItemsById<Body>(ids: Array<string>): Promise<FetchResponse<Body>>;
    getContentItems<Body>(requests: FetchRequest['requests'], parameters?: FetchRequest['parameters']): Promise<FetchResponse<Body>>;
    fetchContentItems<Body>({ requests, parameters, }: FetchRequest): Promise<FetchResponse<Body>>;
}
