import { ContentReferenceMeta } from './ContentMeta';
/**
 * Required params for creating an content reference
 */
export declare type RequiredContentReference<T extends Record<string, unknown> = Record<string, unknown>> = T & {
    id: string;
    contentType?: string;
    _meta?: {
        schema: string;
    };
};
/**
 * Class representing an Content Reference.
 */
export declare class ContentReference {
    id: string;
    contentType: string;
    _meta: ContentReferenceMeta;
    constructor(data: RequiredContentReference);
    /**
     * Export ContentReference to JSON
     */
    toJSON(): any;
    /**
     * @hidden
     * Returns true if the provided fragment is an content reference
     * @param fragment
     */
    static isContentReference(fragment: RequiredContentReference<Record<string, unknown>>): boolean;
}
