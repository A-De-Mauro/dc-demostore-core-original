import { AxiosInstance } from 'axios';
import { ContentClientConfigV2 } from '../../config/ContentClientConfigV2';
import { FilterByRequest, FilterByResponse } from '../model/FilterBy';
export declare class FilterByImpl<Body = any> {
    private readonly config;
    private readonly contentClient;
    constructor(config: ContentClientConfigV2, contentClient: AxiosInstance);
    fetch(requestConfig: FilterByRequest): Promise<FilterByResponse<Body>>;
}
